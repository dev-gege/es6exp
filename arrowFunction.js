let createSomething = function(message, name) {
  return message + name; 
}

// Same function with arrow
let arrowSomething = (message, name) => {
  return message + name 
}

// One liner arrow function
let arrowSomething = message, name => message + name 
